name: Ultra Desk Windows Build

on:
  workflow_dispatch:

jobs:
  build-windows:
    uses: ./.github/workflows/flutter-build.yml
    with:
      upload-artifact: true
      upload-tag: ultradesk-nightly
